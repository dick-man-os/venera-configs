# 9I Chinese Practical Closure — Phase-B

## Classification

9I CLOSED / PASS — THREE CHINESE SOURCES PUSHED — READY FOR WINDOWS SAMPLING

## Actual Rescue Parent

The exact parent is `86bc1a5c5282bccc35d7a4666bee291c7d133f09`. Before implementation, local HEAD, tracking and the actual remote all matched this pushed rescue descendant of `4804fc7ffa83ae6ef9a3cb86acb887c5052746a3`.

## Sources Implemented

| Source | Artifact | Family | Runtime key | Locale | Version |
| --- | --- | --- | --- | --- | --- |
| 瓜子漫画 | `guazimanhua` | `guazimanhua-v1` | `keiyoushi_9103931521355991619` | `zh-Hans` | `1.0.0` |
| 泰拉记事社 | `terrahistoricus` | `terrahistoricus-v1` | `keiyoushi_4585134706567717130` | `zh-Hans` | `1.0.0` |
| 《崩坏3》IP站 | `bh3` | `bh3-v1` | `keiyoushi_5943234929466346733` | `zh-Hans` | `1.0.0` |

## Guazimanhua Contract

The bounded HTML family uses the required desktop User-Agent without Referer or Origin. Search query `我` returned 35 entries on page 1, 36 on page 2 and 22 on current terminal page 75; the terminal result count changed during the milestone while the explicit next-link remained absent, directly confirming that result count cannot define termination. The runtime validates pager identity, rejects repeated or foreign pagination, deduplicates canonical comic IDs, and normalizes 605 chapters once to OLD → NEW. Reader URLs are direct, ordered, unique and absolute.

## TerraHistoricus Contract

The API family merges both fixed topic catalogs, currently 21 + 5 works, while search remains finite and local. It preserves string episode IDs including `0210`, includes main, side-story, preview and special entries, and normalizes 13 chapters to OLD → NEW. Reader output remains a stable API key. `onImageLoad` resolves a fresh signed HTTPS URL asynchronously; full 8-page concurrent preload preserved every stable key and returned eight resolutions without mutation.

## BH3 Contract

The hybrid family reads the complete 23-work DOM catalog, derives numeric book IDs without executing page JavaScript, and performs deterministic local normalized-title filtering. It retrieves the authoritative full 67-chapter JSON rather than the partial detail UI, normalizes chapters OLD → NEW, and extracts only ordered `data-original` reader URLs.

## Live Validation

The generated runtime was executed through the unchanged Windows Host QuickJS and Dart HTML bridge. All 41 metadata requests and all 27 image requests returned HTTP 200. Flutter decoded first, middle and last images for the oldest, middle and latest sampled chapters of every source. Page counts were 19/36/39 for 瓜子漫画, 8/71/46 for 泰拉记事社, and 24/18/22 for 《崩坏3》IP站. The detailed audit excludes transient signed URLs.

## Identity / Locale

All three exact upstream IDs, modules and pinned file hashes were enforced at canonical inventory pin `5a0261c718cd6d5ecf14963d837f29024c792398`. Runtime keys are unique across published, legacy and locale-sibling entries. All three are evidence-backed `zh-Hans` sources. No automatic migration or identity replacement is present.

## Existing Source Regression

漫画屋 and HANMAN18 remain at `1.0.1` with identical runtime bytes. Their 63 targeted family/rescue regressions passed. The accepted HANMAN18 upstream damage at chapter 114 pages 40 and 64 remains outside the 9I failure boundary. English publications, JJK, Black Clover and English MangaCatalog are unchanged.

## Version Table

The three new sources start at `1.0.0`. Every existing source retains its prior version; 漫画屋 and HANMAN18 specifically remain `1.0.1`. No existing runtime byte changed, so no existing version bump was warranted.

## Tests

The untouched rescue parent independently passed 665 tests. The new family suite added 19 fixture-owned contract tests. The final source-conversion suite discovered and passed 684 tests with zero failures and zero errors. `git diff --check` passed.

## Host Anchor

The source-owned Host anchor independently discovered, executed and passed 24 persisted tests against Host `126a462debbe5b05a8c8d6e3dcb1b67fff1ebcdf`. A separate disposable live suite passed 3/3 source cases and 27/27 Flutter image decodes. 9I wrote only to a disposable Host copy. The physical Host HEAD stayed fixed; a final guard observed unrelated concurrent worktree edits, which were left untouched.

## Determinism

Canonical materializer CHECK and WRITE used reviewed digest `23cdced7d9dcbd7d4a1aae17becbece21275965f232eb648ad451d5c5b5c553c`. Each new source regenerated twice identically; generated base equals root JS and no patch exists. SHA-256: `guazimanhua` `6b2b0a114bab9aa43c4682774a67a933907e8e53f27c875d9be8e3b386306fba`; `terrahistoricus` `20190b8759fb8b5b113ce63e759d1d3fe31b9d163ebc04e32b26607b0babebe2`; `bh3` `fbae605ae6c2e3be930a932705e6fddfc022b1212758bb5d94d372960e1a14e1`.

## Blast Radius

All 99 pre-existing runtime, generated and IR files were byte-identical. Current infrastructure regenerated all 35 Phase-A/closed-contract checks identically, with `version bump = NO` and `runtime byte change = NO` for every entry: comicabc, dongmanmanhua_zh_hans, flamecomics, globalcomix_zh_hans, hanman18, mangadex_zh_hans, mangadex_zh_hant, manhuashe, manhuawu, namicomi_zh_hans, namicomi_zh_hant, readblackclovermangaonline, readfairytailedenszeromangaonline, readjujutsukaisenmangaonline, readkingdommangaonline, readnanatsunotaizai7deadlysinsmangaonline, readonepiecemangaonline, readsololevelingmangamanhwaonline, readtokyoghoulretokyoghoulmangaonline, webtoons, webtoons_zh_hant, all.globalcomix:3451257781273481191, all.globalcomix:7151191693036508367, all.namicomi:1163192659786040070, all.namicomi:7859611418350123856, zh.dongmanmanhua:4222375517460530289, zh.iqiyi:2198877009406729694, all.yellownote:170542391855030753, all.yellownote:4899554363948814001, zh.manhuawu:3279300917142951720, zh.miaoqu:116946528518438525, zh.sixmh:5183325399429659419, zh.hanman18:5092568988625041973, all.mangadex:1493666528525752601, all.mangadex:5148895169070562838. The schema change adds only three closed family enum values; dispatch remains exact-module, exact-source-ID and exact-pin constrained.

## Self-Review

The final diff is limited to the three new family contracts, three templates, three IR/base/root triplets, exact registry/index entries, test goldens, one focused test suite, this report and its two audits. Every existing index entry and its formatting is unchanged; the diff only appends three entries before the closing bracket. There are no Host changes, English changes, machine-local paths, cache artifacts, identity migrations or generic semantic changes.

## Candidate Matrix

The canonical successor contains 93 one-to-one identities and zero UNKNOWN: 14 `PUBLISHED_PASS`, 3 `CONVERTED_LIVE_BLOCKED`, 21 `PARKED_ADAPTER`, 41 `PARKED_ARCHITECTURE`, 1 `BLOCKED_AUTH`, 2 `BLOCKED_ANTIBOT`, 1 `DEAD_UPSTREAM` and 10 `DUPLICATE_ROLE`. All values are within the approved state allowlist.

## Practical-Closure Assessment

This batch closes the three Phase-A production selections without opening another adapter family. The remaining 62 adapter/architecture rows do not have the same combined proof of low risk, identity clarity and bounded yield. A final 9J closure review is still useful before declaring Chinese practical closure reached.

## Git Gate

Immediately before staging, local HEAD, tracking and actual remote must still equal the exact rescue parent. Only the reviewed manifest is staged. The cached manifest and full cached diff must pass review and `git diff --cached --check` before the preferred commit `feat(sources): add final Chinese source batch`. Immediately before the one non-force push, the actual remote is checked again against the rescue parent.

## Final State

After publication, local HEAD, tracking and actual remote are required to match; `venera-configs` must be clean with an empty index and no untracked files. VeneraX remains at `126a462debbe5b05a8c8d6e3dcb1b67fff1ebcdf`; unrelated concurrent Host worktree edits remain outside this milestone. The physical upstream checkout remains clean at `5e06c412c0264b18120fd963fdd6efb529f3fa29`. `writer_released=true`.

## Windows Runtime Handoff

Run one bounded sample. In 瓜子漫画, search `我`, move from page 1 to 2 and then open `斗罗大陆`; confirm the large chapter list and open oldest, middle and latest chapters. In 泰拉记事社, open both topic pages, confirm a chapter retaining string ID `0210` and open one signed-reader chapter. In 《崩坏3》IP站, search `第二次崩坏`, confirm the complete chapter list and open one reader chapter. Confirm newest has no Next, middle navigates newer/older correctly, oldest has no Previous, and images render.

## PowerShell Launch Commands

```powershell
cd C:\Projects\VeneraX-Dev\VeneraX
Start-Process ".\build\windows\x64\runner\Debug\venera.exe"
```

If the Debug executable is absent:

```powershell
cd C:\Projects\VeneraX-Dev\VeneraX
flutter run -d windows
```

## Recommended 9J

Run one evidence-only Chinese practical-closure review over the remaining canonical matrix. Reassess whether any single family has gained a bounded anonymous contract and enough unique yield; otherwise close the expansion phase. Do not automatically start another adapter.
